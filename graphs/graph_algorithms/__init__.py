def source_dest_in_graph(graph, source, destination):
    assert source in graph.vertices, "source node isn't in graph"
    assert destination in graph.vertices, "destination isn't in graph"
    return True
